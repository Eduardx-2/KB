services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - backend/.env

  # Frontend: run locally with `cd frontend && npm run dev`,
  # or deploy to Netlify (see frontend/netlify.toml).
